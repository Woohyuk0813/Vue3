1. watch(반응형 데이터,(현재값, 이전값) =>{}, 옵션 : immediate/deep/once/post | sync)함수
  - 반응형 데이터
    * ref()
      반응형 데이터는 반응형 데이터 자체를(변수의 값이 완전히 달라지는 것) 감시(배열이나, 객체의 속성 값 변경은 감시 x)
    * reactive
2. watchEffect()
  - 콜백 함수 내부에서 참조하는 모든 반응형 데이터를 감시한다  옵션스api에서는 제공하지 않는다.
    deep, immediate 옵션이 true 적용되어 있음
    콜백함수의 시점을 결정하는 flush 옵션이 있다.
3. watchPostEffect()

4. props
  컴포넌트에서 다른 컴포넌트로 데이터를 전달할 때 사용하는 속성이다 (별 5개)